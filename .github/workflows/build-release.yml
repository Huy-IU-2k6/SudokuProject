name: Build Sudoku Multi-Platform

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false 
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    # Chỉ cài các gói đồ họa nền tảng (OpenGL, X11) cho Linux. 
    # KHÔNG cài SFML nữa, CMake sẽ tự tải SFML 3.1.0!
    - name: Install Linux Core Dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y libxrandr-dev libxcursor-dev libudev-dev libfreetype-dev libopenal-dev libflac-dev libvorbis-dev libgl1-mesa-dev libegl1-mesa-dev xorg-dev

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=Release

    - name: Build Project
      run: cmake --build build --config Release

    - name: Run CPack
      working-directory: build
      run: cpack -C Release

    - name: Upload Installer
      uses: actions/upload-artifact@v4
      with:
        name: Sudoku-Installer-${{ matrix.os }}
        path: |
          build/*.exe
          build/*.dmg
          build/*.deb
        if-no-files-found: warn